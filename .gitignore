# Local environment (contains DEVRELOS_SECRET_KEY, SMTP and API tokens)
.env
.env.*
!.env.example

# Node
node_modules/
.next/
*.tsbuildinfo

# Bundled at build time from docs/ by apps/web/scripts/generate-docs.mjs.
apps/web/lib/generated-docs.ts

# Regenerated by `next dev` on every run; not part of the project's own docs.
apps/web/AGENTS.md
apps/web/CLAUDE.md

# Go
*.test
*.out
coverage.txt

# Runtime data written by the worker and media pipeline
data/
!data/.gitkeep

# Editor / OS
.DS_Store
.idea/
.vscode/
